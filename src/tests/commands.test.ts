import { describe, it } from 'bun:test';
describe('Commands', () => {
  it('handle = for option value', async () => {});
  it('handle handle space between option and value', async () => {});
  it('get cli version', async () => {});
});
